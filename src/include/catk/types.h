#ifndef __TYPES_H
#define __TYPES_H

#include <stdint.h>

typedef uint16_t  dev_t;   // device numbers
typedef uint16_t  gid_t;   // group ids
typedef uint16_t  mode_t;  // some file attributes
typedef uint32_t  off_t;   // file sizes
typedef int32_t   pid_t;   // process ids
typedef uint32_t  size_t;  // unsigned long (uint32_t)
typedef int32_t   ssize_t; // signed long (int32_t)
typedef uint16_t  uid_t;   // user id

#endif
