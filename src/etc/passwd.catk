/*
 *  CatK's "etc/passwd.catk", used for enumerating and storing user accounts. Uses "root" as default.
 *
*/

char* rootuser = "root:/bin/sh";
char* userid = "root";

// Structure to represent a user entry
struct User {
    char username[50];
    char shell[50];
};